using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SphereJoiner : MonoBehaviour {

    public GameObject From, To;

    private GameObject _joiner;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {

        //if (_joiner == null)
        //{
        //    _joiner = GameObject.CreatePrimitive(PrimitiveType.Capsule);
        //    _joiner.transform.parent = transform;
        //    _joiner.transform.localPosition = Vector3.zero;

        //    var meshFilter = _joiner.GetComponent<MeshFilter>();
        //    //meshFilter.mesh.set
        //}

        //var fromMesh = From.GetComponent<MeshFilter>().mesh;

        //fromMesh.

	}
}
